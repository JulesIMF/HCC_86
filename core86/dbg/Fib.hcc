def Fib with a
{
    ! if | a = 0 |
        ! return 0
    ! if | a = 1 |
        ! return 1
    
    ! return call Fib |a - 2| + call Fib |a - 1|
}

main
{

    ! n is int
    ! n gets 3
    ! call printInt | n |
    ! call newLine ||
    ! i is int  
    ! i gets 0
    
    ! n gets call scanInt ||

    ! while |i =/= n|
    {
        ! i gets i + 1
        ! call printInt | i |
        ! call newLine ||
    }
}